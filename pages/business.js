import Head from 'next/head'
import { Inter } from 'next/font/google'
import styles from '@/styles/Home.module.css'
import Layout from '@/components/Layout'
import Prismic from "prismic-javascript"
import { RichText } from 'prismic-reactjs'
import Link from 'next/link'
import { Client } from "@/prismic-configuration";
import { PrismicText } from '@prismicio/react'
const inter = Inter({ subsets: ['latin'] })

export default function Politics({ article3, trending3 }) {
    return (
        <>
            <Head>
                <title>Create Next App</title>
                <meta name="description" content="Generated by create next app" />
                <meta name="viewport" content="width=device-width, initial-scale=1" />
                <link rel="icon" href="/favicon.ico" />
            </Head>
            <Layout>
                <h1 className='layout_title'>"Corporate Chuckles: Navigating the Boardroom with a Sense of Humor</h1>
                <div className='flex'>
                    <div>
                        <div className='mainArticleBox'>
                            {
                                article3.results.map((article, index) => (
                                    <>
                                        <Link href={`article/${article.uid}`} className='article_title'>
                                            <h1 key={article.uid}>
                                                {article.data.article2title}
                                            </h1>
                                        </Link>

                                        {article.data.slices.map((slice, sliceIndex) => (
                                            <div key={slice.uid}>
                                                {slice.items.map((item, index) => (
                                                    <img src={item.artcile3imageslice.url} width={500} height={300} />
                                                ))}
                                            </div>
                                        ))}
                                        <p className='article_content'>{article.data.arrticle3text}</p>
                                    </>
                                ))
                            }
                        </div>
                    </div>
                    <div className='trending_box'>
                        <div>
                            {trending3.results.map((first, index) => (
                                <>
                                    <div key={first.data.uid}>
                                        <h2 className='trending_title' >{first.data.slices[0].primary.trending3title}</h2>
                                        {first.data.slices.map((second, index) => (
                                            second.items.map((third, index) => (
                                                <div>
                                                    <div key={index} className='trending_list_wrapper'>
                                                        <p> <Link href="./trending/trending" className='trending_list'>{third.trending3list1}</Link><hr /></p>
                                                        <p><Link href="./trending/trending" className='trending_list'>{third.trending3list2}</Link><hr /></p>
                                                        <p> <Link href="./trending/trending" className='trending_list'>{third.trending3list3}</Link><hr /></p>
                                                    </div>
                                                </div>
                                            ))
                                        ))}
                                    </div>
                                </>
                            ))

                            }
                        </div >
                    </div>
                </div>
            </Layout>
        </>
    )
}


// this function is called everytime when request is made or refresh

export async function getServerSideProps() {
    const article3 = await Client().query(Prismic.Predicates.at("document.type", "article3"));
    const trending3 = await Client().query(Prismic.Predicates.at("document.type", "trending3"));
    console.log(trending3)
    return {
        props: {
            article3: article3,
            trending3: trending3,

        }
    }
}